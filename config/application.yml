configserver:
    name: Docker Consul Service
    status: Connected to Consul YX Server running in Docker

jhipster:
    security:
        authentication:
            jwt:
                secret: my-secret-token-to-change-in-production
spring:
    cloud:
        consul:
            host: localhost
            port: 8500
